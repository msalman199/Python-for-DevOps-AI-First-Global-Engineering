Policy Misconfiguration Detection Engine
Prerequisites
Basic understanding of Linux command line
Familiarity with text files and JSON format
Basic knowledge of security policies (firewall rules, access controls)
Comfortable using a text editor (nano, vim, or vi)
Learning Objectives
By the end of this lab, you will be able to:

Understand common policy misconfiguration patterns
Analyze security policies for weak or conflicting rules
Build a basic policy detection engine using Python
Identify insecure policy configurations automatically
Environment Setup
Step 1: Start Your Lab Environment
Click the Start Lab button to provision your Linux machine. Once ready, connect via SSH.

Step 2: Update System and Install Python
sudo apt update
sudo apt install -y python3 python3-pip
Step 3: Create Lab Directory
mkdir ~/policy-lab
cd ~/policy-lab
Step 4: Install Required Python Libraries
pip3 install pyyaml
Task 1: Create Sample Policy Files
Step 1: Create a Firewall Policy File
Create a sample firewall policy with intentional misconfigurations:

nano firewall_policy.json
Add the following content:

{
  "firewall_rules": [
    {
      "rule_id": "FW-001",
      "action": "ALLOW",
      "source": "0.0.0.0/0",
      "destination": "10.0.0.5",
      "port": "22",
      "protocol": "TCP",
      "description": "SSH access from anywhere"
    },
    {
      "rule_id": "FW-002",
      "action": "ALLOW",
      "source": "192.168.1.0/24",
      "destination": "10.0.0.10",
      "port": "443",
      "protocol": "TCP",
      "description": "HTTPS from internal network"
    },
    {
      "rule_id": "FW-003",
      "action": "DENY",
      "source": "0.0.0.0/0",
      "destination": "10.0.0.5",
      "port": "22",
      "protocol": "TCP",
      "description": "Block SSH from anywhere"
    },
    {
      "rule_id": "FW-004",
      "action": "ALLOW",
      "source": "0.0.0.0/0",
      "destination": "10.0.0.20",
      "port": "3389",
      "protocol": "TCP",
      "description": "RDP access from anywhere"
    },
    {
      "rule_id": "FW-005",
      "action": "ALLOW",
      "source": "0.0.0.0/0",
      "destination": "10.0.0.30",
      "port": "*",
      "protocol": "*",
      "description": "Allow all traffic to database server"
    }
  ]
}
Save and exit (Ctrl+X, then Y, then Enter).

Step 2: Create an Access Control Policy File
nano access_policy.json
Add the following content:

{
  "access_rules": [
    {
      "rule_id": "AC-001",
      "user": "admin",
      "resource": "/etc/passwd",
      "permission": "write",
      "description": "Admin can modify password file"
    },
    {
      "rule_id": "AC-002",
      "user": "*",
      "resource": "/var/log/secure",
      "permission": "read",
      "description": "Everyone can read security logs"
    },
    {
      "rule_id": "AC-003",
      "user": "guest",
      "resource": "/root",
      "permission": "read",
      "description": "Guest can read root directory"
    },
    {
      "rule_id": "AC-004",
      "user": "developer",
      "resource": "/home/developer",
      "permission": "read",
      "description": "Developer can read own directory"
    }
  ]
}
Save and exit.

Task 2: Build the Policy Detection Engine
Step 1: Create the Main Detection Script
nano policy_detector.py
Add the following starter code:

#!/usr/bin/env python3
"""
Policy Misconfiguration Detection Engine
Analyzes security policies for weak or conflicting rules
"""

import json
import sys

class PolicyDetector:
    """
    Main class for detecting policy misconfigurations
    """
    
    def __init__(self):
        """Initialize the detector with empty findings list"""
        self.findings = []
    
    def load_policy(self, filename):
        """
        Load policy file and return parsed JSON
        
        Args:
            filename: Path to the policy JSON file
            
        Returns:
            Dictionary containing policy data
        """
        # TODO: Open and read the JSON file
        # TODO: Parse JSON and return the data
        # TODO: Handle file not found errors
        try:
            with open(filename, 'r') as f:
                return json.load(f)
        except FileNotFoundError:
            print(f"Error: File {filename} not found")
            return None
        except json.JSONDecodeError:
            print(f"Error: Invalid JSON in {filename}")
            return None
    
    def check_overly_permissive_source(self, rule):
        """
        Check if firewall rule allows traffic from anywhere (0.0.0.0/0)
        
        Args:
            rule: Dictionary containing firewall rule data
            
        Returns:
            String describing the issue, or None if no issue found
        """
        # TODO: Check if source is 0.0.0.0/0 and action is ALLOW
        # TODO: Return a descriptive message if misconfiguration found
        pass
    
    def check_conflicting_rules(self, rules):
        """
        Check for conflicting firewall rules (same source/dest/port but different actions)
        
        Args:
            rules: List of firewall rule dictionaries
            
        Returns:
            List of conflict descriptions
        """
        # TODO: Compare each rule with every other rule
        # TODO: Check if rules have same source, destination, port, protocol
        # TODO: Check if actions differ (one ALLOW, one DENY)
        # TODO: Return list of conflicts found
        pass
    
    def check_wildcard_permissions(self, rule):
        """
        Check for dangerous wildcard permissions in access rules
        
        Args:
            rule: Dictionary containing access rule data
            
        Returns:
            String describing the issue, or None if no issue found
        """
        # TODO: Check if user is "*" (everyone)
        # TODO: Check if permission is "write" or resource is sensitive
        # TODO: Return descriptive message if issue found
        pass
    
    def check_sensitive_resource_access(self, rule):
        """
        Check if non-admin users have access to sensitive resources
        
        Args:
            rule: Dictionary containing access rule data
            
        Returns:
            String describing the issue, or None if no issue found
        """
        # TODO: Define list of sensitive resources (/etc/passwd, /root, etc.)
        # TODO: Check if user is not admin but has access to sensitive resource
        # TODO: Return descriptive message if issue found
        pass
    
    def analyze_firewall_policy(self, policy_data):
        """
        Analyze firewall policy for misconfigurations
        
        Args:
            policy_data: Dictionary containing firewall rules
        """
        # TODO: Extract firewall_rules from policy_data
        # TODO: For each rule, run check_overly_permissive_source
        # TODO: Run check_conflicting_rules on all rules
        # TODO: Add findings to self.findings list
        pass
    
    def analyze_access_policy(self, policy_data):
        """
        Analyze access control policy for misconfigurations
        
        Args:
            policy_data: Dictionary containing access rules
        """
        # TODO: Extract access_rules from policy_data
        # TODO: For each rule, run check_wildcard_permissions
        # TODO: For each rule, run check_sensitive_resource_access
        # TODO: Add findings to self.findings list
        pass
    
    def generate_report(self):
        """
        Generate and print a report of all findings
        """
        # TODO: Print header
        # TODO: Print each finding with severity level
        # TODO: Print summary statistics
        pass


def main():
    """Main function to run the policy detector"""
    print("=" * 60)
    print("Policy Misconfiguration Detection Engine")
    print("=" * 60)
    
    # Create detector instance
    detector = PolicyDetector()
    
    # TODO: Load and analyze firewall policy
    # TODO: Load and analyze access policy
    # TODO: Generate report
    
    print("\nAnalysis complete!")


if __name__ == "__main__":
    main()
Save and exit.

Step 2: Implement the Detection Functions
Now implement the TODO sections. Here's guidance for each function:

For check_overly_permissive_source:

def check_overly_permissive_source(self, rule):
    if rule.get('source') == '0.0.0.0/0' and rule.get('action') == 'ALLOW':
        port = rule.get('port')
        return f"CRITICAL: Rule {rule['rule_id']} allows traffic from anywhere to port {port}"
    return None
For check_conflicting_rules:

def check_conflicting_rules(self, rules):
    conflicts = []
    for i, rule1 in enumerate(rules):
        for rule2 in rules[i+1:]:
            # Check if rules target same traffic
            if (rule1['source'] == rule2['source'] and 
                rule1['destination'] == rule2['destination'] and
                rule1['port'] == rule2['port'] and
                rule1['protocol'] == rule2['protocol']):
                # Check if actions differ
                if rule1['action'] != rule2['action']:
                    conflicts.append(
                        f"CONFLICT: Rules {rule1['rule_id']} and {rule2['rule_id']} "
                        f"have conflicting actions for same traffic"
                    )
    return conflicts
For check_wildcard_permissions:

def check_wildcard_permissions(self, rule):
    if rule.get('user') == '*':
        return f"HIGH: Rule {rule['rule_id']} grants {rule['permission']} to everyone"
    return None
For check_sensitive_resource_access:

def check_sensitive_resource_access(self, rule):
    sensitive_resources = ['/etc/passwd', '/etc/shadow', '/root', '/var/log/secure']
    resource = rule.get('resource')
    user = rule.get('user')
    
    if resource in sensitive_resources and user not in ['admin', 'root']:
        return f"HIGH: Rule {rule['rule_id']} allows {user} access to sensitive resource {resource}"
    return None
For analyze_firewall_policy:

def analyze_firewall_policy(self, policy_data):
    if not policy_data:
        return
    
    rules = policy_data.get('firewall_rules', [])
    print(f"\nAnalyzing {len(rules)} firewall rules...")
    
    # Check each rule for overly permissive sources
    for rule in rules:
        finding = self.check_overly_permissive_source(rule)
        if finding:
            self.findings.append(finding)
    
    # Check for conflicting rules
    conflicts = self.check_conflicting_rules(rules)
    self.findings.extend(conflicts)
For analyze_access_policy:

def analyze_access_policy(self, policy_data):
    if not policy_data:
        return
    
    rules = policy_data.get('access_rules', [])
    print(f"Analyzing {len(rules)} access control rules...")
    
    for rule in rules:
        # Check for wildcard permissions
        finding = self.check_wildcard_permissions(rule)
        if finding:
            self.findings.append(finding)
        
        # Check for sensitive resource access
        finding = self.check_sensitive_resource_access(rule)
        if finding:
            self.findings.append(finding)
For generate_report:

def generate_report(self):
    print("\n" + "=" * 60)
    print("SECURITY FINDINGS REPORT")
    print("=" * 60)
    
    if not self.findings:
        print("\nNo misconfigurations detected. All policies appear secure.")
        return
    
    print(f"\nTotal Issues Found: {len(self.findings)}\n")
    
    for i, finding in enumerate(self.findings, 1):
        print(f"{i}. {finding}")
    
    # Count severity levels
    critical = sum(1 for f in self.findings if 'CRITICAL' in f)
    high = sum(1 for f in self.findings if 'HIGH' in f)
    
    print("\n" + "-" * 60)
    print(f"Summary: {critical} Critical, {high} High severity issues")
    print("=" * 60)
For main function:

def main():
    print("=" * 60)
    print("Policy Misconfiguration Detection Engine")
    print("=" * 60)
    
    detector = PolicyDetector()
    
    # Analyze firewall policy
    fw_policy = detector.load_policy('firewall_policy.json')
    detector.analyze_firewall_policy(fw_policy)
    
    # Analyze access policy
    ac_policy = detector.load_policy('access_policy.json')
    detector.analyze_access_policy(ac_policy)
    
    # Generate report
    detector.generate_report()
    
    print("\nAnalysis complete!")
Step 3: Make the Script Executable
chmod +x policy_detector.py
Step 4: Run the Detection Engine
python3 policy_detector.py
Expected Output: ```
Policy Misconfiguration Detection Engine
Analyzing 5 firewall rules... Analyzing 4 access control rules...

============================================================ SECURITY FINDINGS REPORT
Total Issues Found: 7

CRITICAL: Rule FW-001 allows traffic from anywhere to port 22
CONFLICT: Rules FW-001 and FW-003 have conflicting actions for same traffic
CRITICAL: Rule FW-004 allows traffic from anywhere to port 3389
CRITICAL: Rule FW-005 allows traffic from anywhere to port *
HIGH: Rule AC-002 grants read to everyone
HIGH: Rule AC-003 allows guest access to sensitive resource /root
HIGH: Rule AC-002 allows * access to sensitive resource /var/log/secure
Summary: 3 Critical, 4 High severity issues
Analysis complete!


## Verification

### Step 1: Verify All Files Are Created

```bash
ls -lh ~/policy-lab/
You should see:

firewall_policy.json
access_policy.json
policy_detector.py
Step 2: Test with Modified Policies
Create a secure policy to verify the detector works correctly:

nano secure_policy.json
Add:

{
  "firewall_rules": [
    {
      "rule_id": "FW-100",
      "action": "ALLOW",
      "source": "192.168.1.0/24",
      "destination": "10.0.0.5",
      "port": "22",
      "protocol": "TCP",
      "description": "SSH from internal network only"
    }
  ]
}
Modify the script to test this file and verify it reports no issues.

Step 3: Check Understanding
Answer these questions:

What makes rule FW-001 critical?
Why do rules FW-001 and FW-003 conflict?
What's wrong with allowing everyone to read /var/log/secure?
Troubleshooting Tips
Issue: "Module not found" error

Solution: Ensure you installed Python packages: pip3 install pyyaml
Issue: "File not found" error

Solution: Verify you're in the correct directory: cd ~/policy-lab
Check files exist: ls -la
Issue: Script doesn't detect issues

Solution: Verify your implementation of detection functions
Add print statements to debug: print(f"Checking rule: {rule}")
Issue: JSON parsing errors

Solution: Validate JSON syntax using: python3 -m json.tool firewall_policy.json
Conclusion
Congratulations! You've successfully built a policy misconfiguration detection engine. You learned to:

Identify common security policy misconfigurations
Detect overly permissive firewall rules (allowing traffic from anywhere)
Find conflicting rules that create security gaps
Spot dangerous access control permissions
Automate security policy analysis
Real-world Applications:

Security audits of cloud infrastructure
Compliance checking for regulatory requirements
Continuous monitoring of policy changes
DevSecOps pipeline integration
Next Steps:

Add more detection rules (weak passwords, encryption settings)
Support additional policy formats (YAML, XML)
Implement severity scoring
Create automated remediation suggestions
This foundational skill is essential for cybersecurity professionals working with cloud security, network security, and compliance automation.
